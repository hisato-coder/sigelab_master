\documentclass[aspectratio=169]{beamer}
\usepackage{luatexja}
\usepackage{luatexja-fontspec}
\usepackage{listings}
\usepackage{xcolor} % コードの色付けに必要
\usepackage{ulem} % 波線を引きたい

\lstset{
    basicstyle=\ttfamily\small,        % 基本のフォントスタイルとサイズ
    keywordstyle=\color{blue}\bfseries, % キーワード（if, returnなど）のスタイル
    commentstyle=\color{green!50!black},% コメントのスタイル
    stringstyle=\color{red},           % 文字列のスタイル
    showstringspaces=false,            % 文字列中の空白を記号にしない
    numbers=left,                      % 行番号を左に表示
    numberstyle=\tiny\color{gray},     % 行番号のスタイル
    stepnumber=1,                      % 1行ごとに番号を振る
    frame=single,                      % コードブロックを実線で囲む（tbにすると上下線のみ）
    breaklines=true,                   % 長い行は自動的に折り返す
    tabsize=4,                         % タブのインデント幅
    captionpos=t                       % キャプション（タイトル）を上に表示
}
\setmainjfont[BoldFont=IPAexGothic]{IPAexGothic}

% Metropolisテーマ
\usetheme[block=fill]{metropolis}

% オーバーフロー警告を調整
\vfuzz=16pt

% フッターにページ番号を表示
\setbeamertemplate{footline}{\hspace*{\fill}\insertframenumber/\inserttotalframenumber\hspace*{1em}\vspace*{0.5em}}

\title{8 解答}
\author{グループ8}
\date{\today}

\begin{document}
\maketitle
\input{q1.tex}

\input{q2.tex}

\input{q3.tex}

\input{q4.tex}

\input{q5.tex}

\end{document}
